# tests

Purpose: regression coverage for kernel behavior, harness contracts, and benchmark workflows.

Contents
- `test_types.py`: Model round-trip, validator, and package import smoke tests.
- `test_harness.py`: Harness contract round-trip and workspace-driver behavior.
- `test_benchmarks.py`: Benchmark runner integration, coding-task execution, and artifact layout.
- Remaining `test_*.py` files: storage, migrations, retrieval, transactions, verification, consolidation, adapters, and controlled coding fairness/report coverage.
