# tests/fixtures/migrations/embedding

Purpose: checked-in embedding-index schema fixtures used to verify compatibility and upgrade handling.

Contents
- `v1.sql`: Embedding index revision 1 snapshot with the initial vector index table and a representative fixture row.
