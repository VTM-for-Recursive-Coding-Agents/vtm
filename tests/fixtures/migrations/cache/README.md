# tests/fixtures/migrations/cache

Purpose: checked-in cache-store schema fixtures used to verify compatibility and upgrade handling.

Contents
- `v1.sql`: Cache store revision 1 snapshot with the initial `cache_entries` table and a representative fixture row.
