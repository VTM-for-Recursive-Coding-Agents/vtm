# tests/fixtures

Purpose: checked-in test assets that should stay stable across runs and support deterministic regression coverage.

Contents
- `migrations/`: SQLite schema snapshots used to prove metadata, cache, and artifact upgrade paths and compatibility checks.
