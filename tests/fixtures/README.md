# tests/fixtures

Purpose: checked-in test assets that should stay stable across runs and support deterministic regression coverage.

Contents
- `migrations/`: SQLite schema snapshots used to prove metadata, cache, artifact, and embedding upgrade paths and compatibility checks.
